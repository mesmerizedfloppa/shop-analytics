# 🛒 FP Shop Analytics

**Проект:** Аналитика интернет-магазина (каталог, корзина, отчёты)  
**Язык:** Python 3.11+  
**Авторы:** Алмаз, Нурдаулет, Бакашар  
**Тип:** Одностраничное функциональное веб-приложение (SPA)

---

## 📘 Описание

Полнофункциональный интернет-магазин с аналитикой, реализованный в **строго функциональном стиле**:

- ✅ Чистые функции без побочных эффектов
- ✅ Иммутабельные структуры данных
- ✅ Композиция и функции высшего порядка
- ✅ Рекурсия вместо циклов
- ✅ Мемоизация для оптимизации
- ✅ Ленивые вычисления (генераторы)
- ✅ Функциональные типы `Maybe` и `Either`
- ✅ FRP (Functional Reactive Programming)
- ✅ Асинхронная обработка данных

---

## 🧩 Структура проекта

```
project/
├── app/
│   └── main.py                 # UI (Streamlit) - 8 страниц
├── core/
│   ├── domain.py               # Иммутабельные модели (dataclass frozen)
│   ├── transforms.py           # Чистые функции, HOF, Maybe/Either
│   ├── recursion.py            # Рекурсивные операции над деревом
│   ├── lazy.py                 # Ленивые генераторы
│   ├── ftypes.py               # Maybe и Either типы
│   ├── compose.py              # Композиция: compose, pipe
│   ├── service.py              # Фасады (Catalog, Order, Analytics)
│   ├── report.py               # Аналитические отчёты
│   ├── frp.py                  # Event Bus (FRP)
│   └── async_ops.py            # Асинхронные операции
├── data/
│   └── seed.json               # 120 товаров, 30 юзеров, 60 заказов
├── tests/
│   ├── test_lab1.py            # Лаба 1: чистые функции
│   ├── test_lab2.py            # Лаба 2: замыкания + рекурсия
│   ├── test_lab3.py            # Лаба 3: мемоизация
│   ├── test_lab4.py            # Лаба 4: Maybe/Either
│   ├── test_lab5.py            # Лаба 5: ленивые вычисления
│   ├── test_lab6.py            # Лаба 6: FRP
│   ├── test_lab7.py            # Лаба 7: композиция
│   └── test_lab8.py            # Лаба 8: async
├── README.md
└── requirements.txt
```

---

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/<your-username>/fp-shop-analytics.git
cd fp-shop-analytics
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Запуск приложения

```bash
streamlit run app/main.py
```

Приложение будет доступно по адресу: **http://localhost:8501**

### 4. Запуск тестов

```bash
# Все тесты
pytest -v

# Конкретная лаба
pytest tests/test_lab4.py -v

# С покрытием
pytest --cov=core
```

### 5. Проверка стиля кода

```bash
# Форматирование
black .

# Линтинг
ruff check .
```

---

## 💡 Используемые технологии

| Технология | Назначение |
|-----------|------------|
| **Python 3.11+** | Основной язык |
| **Streamlit** | UI (8 страниц) |
| **pytest** | Тестирование (40+ тестов) |
| **functools** | HOF, lru_cache |
| **asyncio** | Параллелизм |
| **dataclasses** | Иммутабельные модели |
| **black + ruff** | Форматирование |

---

## 🧭 Ключевые принципы ФП

### 1. Иммутабельность

```python
@dataclass(frozen=True)
class Cart:
    id: str
    user_id: str
    items: Tuple[Tuple[str, int], ...]  # Неизменяемый кортеж
```

### 2. Чистые функции

```python
def add_to_cart(cart: Cart, product_id: str, qty: int) -> Cart:
    """Возвращает НОВУЮ корзину, не мутирует входную"""
    updated_items = cart.items + ((product_id, qty),)
    return Cart(id=cart.id, user_id=cart.user_id, items=updated_items)
```

### 3. HOF и композиция

```python
# Замыкание-фильтр
def by_price_range(min_p: int, max_p: int) -> Callable[[Product], bool]:
    return lambda p: min_p <= p.price <= max_p

# Композиция
filtered = compose(
    lambda prods: filter(by_price_range(1000, 5000), prods),
    tuple
)(products)
```

### 4. Maybe/Either вместо исключений

```python
def safe_product(products, pid: str) -> Maybe[Product]:
    found = next((p for p in products if p.id == pid), None)
    return Maybe.some(found) if found else Maybe.nothing()

# Использование
result = safe_product(products, "p1")
if not result.is_none():
    product = result.get_or_else(None)
```

### 5. Ленивые вычисления

```python
def iter_orders_by_day(orders: Iterable[Order], day: str) -> Iterator[Order]:
    """Генератор — материализация только при необходимости"""
    for order in orders:
        if order.ts.startswith(day):
            yield order
```

### 6. FRP (Event Bus)

```python
bus = EventBus()
bus = bus.subscribe("ADD_TO_CART", handle_add_to_cart)  # Чистая функция
new_state = bus.publish(event, current_state)  # Иммутабельно
```

---

## 📊 Меню в UI

| Страница | Описание | Лабы |
|----------|----------|------|
| **📊 Overview** | Общая статистика, метрики | 1 |
| **🏪 Каталог** | Фильтрация товаров (замыкания, рекурсия) | 1, 2 |
| **🛒 Корзина** | Управление корзиной, checkout | 1, 4 |
| **📈 Статистика** | Сегментация, retention, ленивые вычисления | 5 |
| **📑 Reports** | Бестселлеры, топ клиенты, Maybe/Either | 3, 4 |
| **⚡ FRP Events** | Reactive programming, event bus | 6 |
| **🚀 Async Analytics** | Параллельная аналитика | 8 |
| **🧪 Tests Demo** | Демонстрация тестов | 1-8 |

---

## 🎯 Реализованные лабораторные

### ✅ Лаба 1: Чистые функции + иммутабельность (4б)

- Загрузка данных: `load_seed()`
- Операции с корзиной: `add_to_cart()`, `remove_from_cart()`, `checkout()`
- Агрегация: `total_sales()` через `reduce`
- **Тесты**: `test_lab1.py` (5 тестов)

### ✅ Лаба 2: Замыкания + рекурсия (4б)

- Замыкания-фильтры: `by_category()`, `by_price_range()`, `by_tag()`
- Рекурсия: `flatten_categories()`, `collect_products_recursive()`
- **Тесты**: `test_lab2.py` (7 тестов)

### ✅ Лаба 3: Мемоизация (4б)

- Кэширование: `@lru_cache` для `top_products()`
- Замеры времени: до/после кэша
- **Тесты**: `test_lab3.py` (5 тестов)

### ✅ Лаба 4: Maybe/Either (4б)

- Типы: `Maybe`, `Either` с `map`, `bind`, `get_or_else`
- Функции: `safe_product()`, `validate_order()`
- **Тесты**: `test_lab4.py` (7 тестов)

### ✅ Лаба 5: Ленивые вычисления (4б)

- Генераторы: `iter_orders_by_day()`, `lazy_top_customers()`
- Потоковая обработка без материализации
- **Тесты**: `test_lab5.py` (5 тестов)

### ✅ Лаба 6: FRP (4б)

- EventBus: `ADD_TO_CART`, `CHECKOUT`, `REFUND`, `REMOVE`
- Чистые обработчики событий
- Живое состояние в UI
- **Тесты**: `test_lab6.py` (6 тестов)

### ✅ Лаба 7: Композиция (4б)

- Функции: `compose()`, `pipe()`
- Фасады: `CatalogService`, `OrderService`, `AnalyticsService`
- DI и композиция методов
- **Тесты**: `test_lab7.py` (5 тестов)

### ✅ Лаба 8: Async/Parallel (4б)

- Асинхронные функции: `sales_by_day_async()`, `sales_by_user_async()`
- Параллельный пайплайн: `comprehensive_analysis_pipeline()`
- `asyncio.gather()` для параллелизма
- **Тесты**: `test_lab8.py` (6 тестов)

---

## 🧪 Тестирование

### Статистика

- **Всего тестов**: 46
- **Покрытие**: ~85% core модулей
- **Время выполнения**: <2 сек

### Запуск

```bash
# Все тесты
pytest -v

# Конкретная лаба
pytest tests/test_lab3.py -v

# С выводом print
pytest -v -s

# С покрытием
pytest --cov=core --cov-report=html
```

---

## 📈 Примеры использования

### Фильтрация товаров

```python
# Композиция фильтров
filters = [
    by_category("c001"),
    by_price_range(1000, 5000),
    by_tag("tech")
]

filtered = tuple(
    filter(lambda p: all(f(p) for f in filters), products)
)
```

### Асинхронная аналитика

```python
import asyncio
from core.async_ops import run_async_pipeline

result = run_async_pipeline(orders, products, users)
print(result["top_products"])  # Топ товаров
print(result["customer_segments"])  # Сегменты
```

### FRP Events

```python
from core.frp import create_shop_event_bus, create_event, initial_state

bus = create_shop_event_bus()
state = initial_state()

# Публикуем событие
event = create_event("ADD_TO_CART", {"cart_id": "c1", "product_id": "p1", "qty": 2})
new_state = bus.publish(event, state)

print(new_state["active_carts"])  # {'c1': {'p1': 2}}
```

---

## 🚀 Roadmap (возможные улучшения)

- [ ] Добавить персистентность (сохранение в БД)
- [ ] Расширить EventBus (replay events)
- [ ] Добавить Property-based тестирование (Hypothesis)
- [ ] Реализовать Free Monad
- [ ] Интеграция с внешним API

---

## 📝 Лицензия

MIT License

---

## 👥 Авторы

- **Алмаз** - Лабы 1-3, UI
- **Нурдаулет** - Лабы 4-6, FRP
- **Бакашар** - Лабы 7-8, async

---

## 📚 Ссылки

- [Streamlit Docs](https://docs.streamlit.io)
- [Python Functional Programming](https://docs.python.org/3/howto/functional.html)
- [asyncio Documentation](https://docs.python.org/3/library/asyncio.html)

---

**⭐ Если проект был полезен, поставьте звезду на GitHub!**