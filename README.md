# FP Shop Analytics

**Проект:** Аналитика интернет-магазина (каталог, корзина, отчёты)  
**Язык:** Python 3.11+  
**Авторы:** Алмаз, Нурдаулет, Бакашар  
**Тип:** Одностраничное функциональное веб-приложение (SPA)

---

## 📘 Описание

Проект моделирует работу интернет-магазина: товары, категории, пользователи, корзины, заказы, оплаты, отчёты и аналитику.  
Основная цель — применение **функционального программирования (FP)** в Python:
- чистые функции  
- иммутабельные данные  
- рекурсия  
- лямбда и HOF  
- мемоизация  
- ленивые вычисления  
- функциональные типы `Maybe` и `Either`

UI реализован на **Streamlit**, тестирование — **pytest**, стиль — **black + ruff**.

---

## 🧩 Структура проекта

project/
app/
main.py # UI (Streamlit)
core/
domain.py # Иммутабельные модели данных
transforms.py # Чистые функции и HOF
recursion.py # Рекурсивные операции
memo.py # Кэширование (мемоизация)
ftypes.py # Функциональные типы Maybe / Either
report.py # Отчёты и аналитика
data/
seed.json # Исходные тестовые данные
tests/
test_transforms.py
test_lab2.py
test_lab4.py
README.md
requirements.txt

---

## ⚙️ Установка и запуск

```bash
git clone https://github.com/<your-username>/shop-analytics.git
cd shop-analytics
pip install -r requirements.txt
streamlit run app/main.py

После запуска приложение будет доступно по адресу:
👉 http://localhost:8501

---

## Тесты 
pytest -q
# или выборочно
pytest tests/test_lab2.py
black . --check
ruff check .

💡 Используемые технологии

Python 3.11+

Streamlit — визуальный интерфейс

pytest — тестирование

functools / itertools — функциональные операции

dataclasses — иммутабельные модели

lru_cache — мемоизация

black, ruff — формат и стиль

🧭 Ключевые принципы FP

Функции не имеют побочных эффектов

Нет мутации данных — только новые структуры

Использование map, filter, reduce вместо циклов

Композиция и замыкания

Ленивые вычисления и кэширование

📊 Меню в UI

Overview — агрегаты (#users, #products, #orders, total sales)

Каталог — фильтрация по категориям, тегам, цене

Корзина — добавление, удаление, оформление заказа

Статистика — отчёты и аналитика продаж

Reports — мемоизация и безопасные операции (Maybe, Either)